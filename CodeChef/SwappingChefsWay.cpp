#include<bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin >> T;
    while(T--) {
        int n;
        char ch[1000];
        cin >> n;
        cin.ignore();
        cin.getline(ch, n);
        string str1 = ch;
        sort(ch, ch + n);
        string str2 = ch;
        for(int i = 0; str1[i] != NULL; i++) {
            
        }
    }
}
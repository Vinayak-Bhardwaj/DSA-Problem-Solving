#include<bits/stdc++.h>
using namespace std;
int main() {
    int test;
    cin >> test;
    while(test--) {
        int n, arr[10000];
        cin >> n;
        for(int i = 0; i < n; i++) {
            cin >> arr[i];
        }
        
    }
}